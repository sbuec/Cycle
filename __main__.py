from services.keyboard_service import KeyboardService as ks
from services.video_service import VideoService as vs

from shared.color import Color
from shared.point import Point

from casting.actor import Actor
from casting.cast import Cast

from directing.director import Director

FRAME_RATE = 12
MAX_X = 900
MAX_Y = 600
FONT_SIZE = 15
CELL_SIZE = 15 # Same as FONT_SIZE
COLS = 60
ROWS = 40       # Number of cols & rows == MAX_X/MAX_Y
CAPTION = "Cycle"
WHITE = Color(255, 255, 255)


def main():
    print('s')

if __name__ == "__main__":
    main()
